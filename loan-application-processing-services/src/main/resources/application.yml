server:
  port: 5055
  
spring:
  application:
    name: loan-application-processing-service
  
  

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka